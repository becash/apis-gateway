type: object
description: Option or time‐slot selection for orders.
required:
  - optionCode
properties:
  optionCode:
    type: string
    enum:
      - Option
      - Time_Slot
    description: |
      Option code. GGT supports "Option" or "Time_Slot".
