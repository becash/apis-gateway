type: object
required:
  - passengerId
  - passengerStatus
properties:
  passengerId:
    type: string
    description: Passenger number.
    example: "PAX-001"
  passengerStatus:
    type: integer
    enum: [0, 1, 2]
    description: 0=Pending, 1=Used, 2=Canceled.
    example: 1
