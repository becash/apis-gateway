type: object
required:
  - header
properties:
  header:
    $ref: './CommonResponseHeader.yaml'
  body:
    $ref: './QueryOrderResponseBody.yaml'
    description: >
      Present when header.resultCode == "0000". Omitted otherwise.
description: |
  Order inquiry response. On success (resultCode "0000"), `body` is required.
