# components/schemas/BookingCutOffTime.yaml
type: object
description: When ordering must cut off for this option
properties:
  dayBeforeVisitDate:
    type: string
    description: Number of days before visit date
  time:
    type: string
    description: Time of day (HH:mm) on that cutoff day
required:
  - dayBeforeVisitDate
  - time
additionalProperties: false
