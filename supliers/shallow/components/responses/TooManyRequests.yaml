description: Too many requests, please try again
content:
  application/json:
    schema:
      type: object
      required:
        - header
      properties:
        header:
          type: object
          required:
            - resultCode
            - resultMessage
          properties:
            resultCode:
              type: string
              description: Error code
            resultMessage:
              type: string
              description: Error message